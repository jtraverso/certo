{
  "_comment": [
    "A linear program as DATA. `certo opt examples/lp_as_data.json` runs it",
    "with nothing compiled and nothing executed -- which is the point: a spec",
    "written by an agent is a program, and most of the corpus does not need",
    "to be one.",
    "",
    "It guarantees exactly that no code from this file runs. It does NOT",
    "guarantee the program means what you meant: `lint` and the scope",
    "warnings are what work on that, and `--safe` is not a substitute for",
    "reading your own spec.",
    "",
    "Numbers are exact or refused. \"1/3\" is a Fraction; 0.333 is rejected,",
    "because a float here is a float in the certificate and `verify` would",
    "call it not citable."
  ],
  "type": "LPSpec",
  "title": "a packing written as data, not as code",
  "sense": "max",
  "var_names": ["x", "y"],
  "bounds": {"x": [0, 10], "y": [0, 10]},
  "obj": {"x": 2, "y": 3},
  "cons": [
    ["cap_a", {"x": 1, "y": 1}, "<=", 1],
    ["cap_b", {"x": 1, "y": 2}, "<=", 1]
  ]
}
